# Workspace Instructions

- When code changes are made for this repository, create a sensible git commit by default after validating the change.
- Do not create a commit for read-only exploration, reviews, or when the user explicitly asks not to commit.
- Prefer small, coherent commits with messages that describe the functional change.